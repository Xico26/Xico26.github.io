{
  "dependencies": {
    "@tailwindcss/cli": "^4.2.4",
    "tailwindcss": "^4.2.4"
  },
  "scripts": {
    "dev": "npx @tailwindcss/cli -i styles.css -o output.css --watch",
    "build": "npx @tailwindcss/cli -i styles.css.css -o output.css --minify"
  }
}
